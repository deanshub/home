networks:
  caddy:
    external: true

services:
  caddy:
    # build: .
    image: ghcr.io/deanshub/casa-caddy:main
    restart: unless-stopped
    environment:
      - TZ=Asia/Jerusalem
    container_name: caddy
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - .:/etc/caddy/
      - ./site:/srv
      - ../../config.yaml:/srv/config.yaml
      - /media/external/library/caddy:/data/
      - /media/external/configs/caddy:/config/
    env_file:
      - .env
    networks:
      - caddy
    command:
      - sh
      - -c
      - "apk add --no-cache yq && yq -o=json /srv/config.yaml > /srv/config.json && caddy run --config /etc/caddy/Caddyfile"
